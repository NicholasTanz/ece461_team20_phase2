{
  "name": "url-processor",
  "version": "1.0.0",
  "description": "CLI for processing URLs and fetching metadata from npm and GitHub",
  "main": "dist/run.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/run.js"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "dotenv": "^16.4.5",
    "ndjson": "^2.0.0",
    "simple-git": "^3.19.1"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
